{
  "name": "11tyhw",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "eleventy --watch --serve",
    "build": "eleventy",
    "deploy": "eleventy && firebase deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0"
  },
  "dependencies": {
    "@11ty/eleventy-fetch": "^3.0.0"
  }
}
